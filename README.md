# fastly-inspect-lambda

[fastly-inspect](https://github.com/yannh/fastly-inspect) as an executable lambda.

The lambda is supposed to be manually executed and just returns the output of `fastly-inspect`.
